import chai, { expect } from 'chai'
import hardhat from 'hardhat'
import { solidity } from 'ethereum-waffle'

describe('DePayRouterV1PancakeSwap01 on BSC', function() {

  beforeEach(()=>{
  })

  it('deploys the plugin', async () => {
  })

  it('approves the plugin', async () => {
  })

  it('swaps BNB for BUSD', async () => {
  })

  it('swaps token for token and performs payment', async () => {
  })

  it('swaps BUSD for ETH', async () => {
  })
})
